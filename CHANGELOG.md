# Changelog

Notable changes, newest first. Facts trace to the git history.

## 2026-07-12 — Backlog build-out + design elevation run

- **Hygiene sprint:** backlog inventory and audit (`BACKLOG.md`, `AUDIT.md`),
  run plan, blocker parks, then the art-floor + accessibility fixes
  (P0-3/P0-4, X-1..X-3, Z-9/Z-10).
- **E-0 groundwork:** player model cut 94.7% (rogue.glb 3.6 MB → 190 KB), the
  single 3.6 MB JS chunk code-split, weather tints in the sky, camera language
  for arena/ego/rite, celebration staging, panel typography pass.
- **Per-world set-pieces (E-1..E-8):** each world got its own ground tints and
  a bespoke landmark; feel packs captured and archived.
- **Mechanics batch (M-1/M-2/M-4/M-5/M-6):** arenas in every world,
  telegraphs, pillars, candles; the Ego's heavy attack speaks the founder's
  own divergence.
- **Procedural audio (A-1):** per-world ambient airs and UI cues synthesized
  in WebAudio; silence is the default until the player opts in.
- **Field Mode (F-1..F-4, F-7):** hunt slots, logged-before-outcome attempts,
  momentum decay with trough freeze, Field Day lifecycle, an import pipeline
  with seven integrity rules, and the Field Mode surface (hunt list, lantern,
  Field Day, the Beam).
- **Council temple (C-1):** key entry, consent, and the by-hand reading —
  browser-to-Anthropic direct, BYOK; a key-placeholder repo-guard trip was
  fixed in favor of the guard.
- **QR beam (F-8):** vendored MIT qrcode-generator encodes FQB1 frames;
  scanning falls back from the native BarcodeDetector to file/paste import.
- **PWA (F-9):** hand-rolled service worker, add-to-home-screen,
  transfer-first honesty.
- **Chart + HUD polish (E-9):** label crowding fixed, per-world pips, chip
  clamp.
- **Raven + credits (E-10/E-11):** First Light v2 with a stylized in-engine
  raven guide, and an in-game attribution page.

## 2026-07-11 and earlier — Prior state

- **The 8-world spine**, already shipped before this run: traversal across all
  eight worlds, per-world sky identity, Act Gates and named-loop toll-portals,
  sequence locks, and the campfire furniture hub — functionally complete per
  the LOOP J17/J18 markers.
- **Program Addendum A "Mind & Myth" (A1–A5)**, already shipped: the WIKJ v3
  epistemology canon, the Earned Hunch, the First Light induction with
  Chart (M) and Legend (L), the Proving Circle confrontation and funeral rite,
  and the Ego — a World 8 boss derived from the founder's own record.

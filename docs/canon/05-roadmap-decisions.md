# 05 · Roadmap & Decision Log
*What's shipped, what's queued, and the decisions that must not be silently re-litigated. Update in the same turn as the change.*

## Shipped (as of 2026-07-05)
- **v3 "the crucible":** full question bank with progressive Socratic ladders and typed answers · Assumption Registry with kill criteria (tier derived from linked evidence) · Evidence Ledger E0–E4 · dual Truth/Action + XP (invalidation 1.5×) · three soft Act Gates with logged overrides · the Vault with solution-language detection · Ariadne's Thread sealing · loop rituals · Quest Brief + Journal exports · v2 data migration.
- **The Council:** consent, live/pasted tabs, thin-ink guard, durable follow-ups via journal snapshots, readings persisted and exported, single serializer with the compact-readings divergence.
- **PIE integration (tiers 1–3 app-side):** commitment gate (one-way feedback) · The Fellowship block · whiplash ritual in the Ledger · Reset retrospective + undefended Critique the Quest · Council cadence paragraph · Weather + trough detection (Shadow holds fire; normalizing banner) · Side Quests (404, Obituary, Fan Letter, Swap) · Dinner Card + "going wrong" leading the Brief · Family Dinner facilitator mode (rules, buckets, timer, spoke-tracking, serializer exclusion).
- **Standalone port:** storage ladder (artifact→localStorage→memory) · Council environment routing · Vite/React/Tailwind repo, build-verified (~87KB gz) · Pages Function with Fable-5 enforcement and zero body logging *(Council path superseded by the 2026-07-08 BYOK decision — see decision log)* · security `_headers` · deploy runbook.
- **Documents:** v3 upgrade prompt · Council build prompt (incl. Mode A companion prompt + Mentor/Shadow persona prompts) · executive overview · positioning (README paragraph + cohort variant) · PIE×FQ analysis · LaunchBase Selection Kit · cookbook scaffold (README, license split, CONTRIBUTING w/ trust ladder, GOVERNANCE, templates, Ariadne's Thread example recipe, mistakes folder).

## Queued (prompts ready — feed to Claude Code)
1. **The Earned Hunch** — Mind & Myth Program Addendum A, phase A2 (supersedes `cc-prompt-earned-hunch.md` as the build prompt). Prereq: `docs/epistemology/wisdom-intuition-knowledge-judgment-v3.md` in the repo — **landed 2026-07-11 (A1)**. Hunch provenance (Earned/Adjacent/Wild/Borrowed), wicked-domain flag, seeded-guardian priority bump, calibration record ("your gut's record"), Council addendum, Guide section, cookbook recipe.
2. **i18n, 40 locales** — `cc-prompt-i18n-40-locales.md`. Prereq: wcjbt repo reachable (`src/config/i18n.mjs` verbatim). i18next namespaces, es-MX first per CDMX register norms, freshness/stamp/security-gate port, CODEOWNERS, RTL, the in-app freshness banner. **Consequential fork: already executed by the 2026-07-08 decision-log entry “The game build ends artifact code parity — announced, not drifted” (the greenfield game replaced `App.jsx`; the artifact is the en-only sandbox); this line now records the consequence rather than requiring fresh approval.**
3. **BYOK Council port** — created by the 2026-07-08 decision; no standalone prompt, lands with the kickoff's standalone build phases. Key-entry UI + visible remove control · key under its own storage key (never inside `founders-quest:v3`, never serialized or exported) · client-code `claude-fable-5` pin · CORS opt-in header on direct `api.anthropic.com` calls · CSP `connect-src` restricted to `'self'` + `api.anthropic.com` · key/consent/key-failure copy from `04-council.md`. Until it ships, `02-architecture`'s Council call contract describes target state, not current code.

## Next after those (specs exist in the build docs; prompts to be crafted here)
Mentor & Shadow in-app AI (persona system prompts already written in the v3 upgrade doc) · Interview Debrief with Mom Test lint · full Story Forge (evidence-locked spine casting) · pre-mortem ritual at gates (partially covered by the Obituary quest) · Five-Whys visual chain · onboarding tour teaching tiers/gates · cookbook extraction of the full bank into recipes · Family Dinner shared-board exploration (privacy-first; must pass the confidentiality wall) · self-hosting recipe (any static host; Caddy example — nothing server-side remains to port after the 2026-07-08 BYOK decision) for the cookbook.

## Decision log (dated; re-opening any of these requires naming the entry)
- **2026-07-11 · Mind & Myth Gate 0 (Program Addendum A) — all decision blocks ratified at the audits' defaults, no exceptions.** D-A confrontations per-world / funeral rite on any invalidation (s5-l5 stays canonical and the rite generalizes it) · D-B Ego = boss-form of the ROAMING Shadow only; the Council's Shadow voice never fights (glossary in 02) · D-C the action layer never gates evidence (idle-timer auto-window; skill accelerates, never locks — tested invariant) · D-D validated guardians transform into standing pillars, equal-lavish celebration · D-E the Council does NOT voice the Ego in this program (pre-written dialogue; zero new API surface) · D-F trough at World 8 delays the fight OFFER, never blocks · D-G firstLight-tagged assumptions excluded from the Truth denominator, fixed First-Light XP (explicit carve-out) · D-H the Raven is the First-Light mentor; the Chart carries the offline/no-cost consent line · D-I the opening registers the first assumption via a DISTINCT elicitation (s1-l1 untouched) · D-J zero telemetry; local-only counters · D-K map trough dip W4–W6 approved · D-L keybinds M/L confirmed · D-M provenance never gates capture (two taps, zero justification; tag optional + post-capture — tested invariant). The binding art law for feel checkpoints is `docs/build/art-direction.md` (B-2). Gate-2 re-pass is PENDING operator play; A3–A5 remain blocked until then (B-3).
- **2026-07-11 · Scanner endpoints ratified (5-rider queue, item 5).** osv-scanner's own endpoints (api.osv.dev in CI; storage.googleapis.com offline DB locally) are covered by the standing "osv-scanner and gitleaks" scanning sanction — recorded per the Phase 1 review's operator-ratification item.
- **2026-07-10 · Spine per-beat citations (OQ3, R-S) — keep the flat interim; no 02 amendment now.** `s8-th` cites evidence as a whole via the existing `citedEvidenceIds[]`; every inked beat renders **[unproven]** until ≥1 citation exists (warn, never block). A per-beat→evidence map stays a clean, isolated later upgrade — drafted as a 02 diff for approval — taken only when the launch story must survive per-beat scrutiny. Interim shipped + tested 2026-07-10.
- **2026-07-10 · Weather same-day retap (OQ5, R-W) — append every tap.** The totem records every tap to `weather[]`; a second same-day tap neither replaces the entry nor locks the totem. The trough keeps its canon last-3-by-date window, so same-day taps each count (accepted: a bad-then-better day can shift when the Shadow is withheld). Reverses the working "replace" interim. Device-local, never transmitted.
- **2026-07-08 · Council model: `claude-fable-5` pinned primary; `claude-sonnet-4-6` fallback on model-access failure — player-accepted, readings labeled.** Extends the 2026-07-08 BYOK entry; further narrows "Fable 5 is enforced server-side only" (2026-07-05, already superseded). Some player keys cannot access the pinned model; a hard pin would dead-end them. On a model-access failure the game makes a one-tap offer of the fallback sage; the player's acceptance persists; every reading records and displays the model that produced it. No free model choice ships in the UI — this single ruled, labeled, player-accepted fallback is the only fork.
- **2026-07-08 · BYOK — the game is for the general public; every player brings their own Anthropic API key.** Re-opens, by name: *"Fable 5 is enforced server-side only"* (2026-07-05) and narrows *"Production = Cloudflare Pages + Functions"* (2026-07-05). The key is entered in-app, lives device-side (localStorage behind consent, its own storage key, never serialized or exported, removable), and travels only browser→`api.anthropic.com` (CORS opt-in header). No server-side `ANTHROPIC_API_KEY` exists anywhere — owner, dev, or prod. `claude-fable-5` stays the pinned Council model, now pinned in client code; a tamperer affects only their own key and bill. Production is pure static; the Pages Function leaves the Council path.
- **2026-07-08 · The game build ends artifact code parity — announced, not drifted.** Re-opens, by name: *"The artifact is the English-only dev sandbox after the i18n fork"* (2026-07-05). The greenfield 3D game (Vite/TS/R3F per 02's amended stack) replaces `src/App.jsx`; byte-parity with `founders-quest-v3.jsx` ends now rather than at the i18n fork. The artifact stays the English-only sandbox and the schema/content reference: `founders-quest:v3`, the metrics, `buildJournalMd`, and `COUNCIL_SYSTEM_PROMPT` remain shared canon, byte-parity-tested where they exist in code.
- **2026-07-05 · Milestone checks don't migrate across changed criteria.** A checkmark carried onto a different claim is a fabricated fact.
- **2026-07-05 · Guardian tiers are derived, never declared.** A founder doesn't grade their own proof.
- **2026-07-05 · Hunches never move Truth.** Standing changes come as *test priority* and calibration, never as evidence weight (constitution-level; reaffirmed by the Earned Hunch spec).
- **2026-07-05 · Dinner data is excluded from all serialization.** Confidentiality is structural, not policy.
- **2026-07-05 · Readings store their journal snapshot.** Durable follow-ups over storage thrift; compact-readings divergence documented in `buildJournalMd`.
- **2026-07-05 · Download-dialog "fix" reverted.** A toast under a platform modal helps no one; the real fix is standalone deployment.
- **2026-07-05 · Production = Cloudflare Pages + Functions; not GitHub Pages (splits the architecture), not Firebase (data-maximization platform hosting a data-minimization app).** Sovereignty through portability: static output + plain fetch handler + founder-browser storage = 30-day exit plan exists on day one. *(Narrowed 2026-07-08: BYOK removes Functions from the Council path; Pages hosting stands, and the exit plan gets simpler still.)*
- **2026-07-05 · Fable 5 is enforced server-side only.** No model choice ships to the browser; the artifact runtime keeps its pinned model. *(Superseded 2026-07-08: BYOK — the pin moves into client code; still no model choice in the UI.)*
- **2026-07-05 · The artifact is the English-only dev sandbox after the i18n fork.** Byte-parity ends by design, announced, not drifted into.
- **2026-06 · PIE Cookbook adopted as ritual source (CC BY 4.0, attributed).** Borrow-test standing rule: every feature must increase human contact, never simulate it.

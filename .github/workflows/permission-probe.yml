# Temporary probe: verifies this environment's git credentials carry the
# workflows permission (setup guide §1 gotcha). Manual-trigger only; deleted
# in the next commit once the push result is recorded.
name: permission-probe
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
